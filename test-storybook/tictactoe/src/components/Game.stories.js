import React from 'react';
import Game from './Game';
import '../styles.css';

export default {
    component: Game,
    title: 'Game',
};

const Template = () => <Game />;

export const Default = Template.bind({});
